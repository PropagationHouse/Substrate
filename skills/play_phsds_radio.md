---
name: play_phsds_radio
description: Searches for and plays PHSDS Radio on YouTube efficiently.
triggers: phsds radio,play phsds radio,start phsds radio
---

# Play PHSDS Radio

This skill automates the process of playing the PHSDS Radio stream on YouTube.

## Instructions
1. Use the `search_youtube` tool with the query "phsds radio".
2. Ensure `click_first` is set to `true` (default behavior).
3. Do not perform follow-up screenshots or redundant searches if the initial call succeeds, as the first result is typically the intended playlist or live stream.

## Triggers
- "play phsds radio"
- "start phsds radio"
- "phsds radio"

